{% extends 'base.html.twig' %}

{% block title %}Chef{% endblock %}

{% block body %}
    <h1>Chef</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chef.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ chef.Prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chef.Nom }}</td>
            </tr>
            <tr>
                <th>Specialitee</th>
                <td>{{ chef.Specialitee }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ chef.Description }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ chef.Contact }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ chef.Image }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ chef.Titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chef_index') }}">back to list</a>

    <a href="{{ path('chef_edit', {'id': chef.id}) }}">edit</a>

    {{ include('chef/_delete_form.html.twig') }}
{% endblock %}
