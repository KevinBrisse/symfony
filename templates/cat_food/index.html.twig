{% extends 'base.html.twig' %}

{% block title %}CatFood index{% endblock %}

{% block body %}
    <h1>CatFood index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Menu</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cat_food in cat_foods %}
            <tr>
                <td>{{ cat_food.id }}</td>
                <td>{{ cat_food.Menu }}</td>
                <td>{{ cat_food.Description }}</td>
                <td>
                    <a href="{{ path('cat_food_show', {'id': cat_food.id}) }}">show</a>
                    <a href="{{ path('cat_food_edit', {'id': cat_food.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cat_food_new') }}">Create new</a>
{% endblock %}
